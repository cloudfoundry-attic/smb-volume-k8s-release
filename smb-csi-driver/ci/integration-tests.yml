---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfpersi/smb-broker-k8s-tests
    tag: latest

inputs:
  - name: smb-volume-k8s-release

run:
  path: make
  args: [ "--directory=smb-volume-k8s-release/smb-csi-driver", "start-docker", "image-local-registry", "test", "kill-docker" ]
