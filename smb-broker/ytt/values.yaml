#@data/values
---
smbBrokerUsername: ""
smbBrokerPassword: ""

namespace: cf-workloads
replicaCount: 1
smbbroker:
  fullname: smb-broker
  name: smb-broker

image:
  repository: cfpersi/smb-broker
  tag: 0.143.0
  pullPolicy: Always

ingress:
  enabled: true
  hosts:
    - host: localhost
      paths:
        - "/v2"

service:
  type: ClusterIP
  port: 80
