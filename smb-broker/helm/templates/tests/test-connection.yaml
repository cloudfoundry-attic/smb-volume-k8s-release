apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smb-broker.fullname" . }}-test-connection"
  labels:
{{ include "smb-broker.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "smb-broker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
